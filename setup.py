from setuptools import find_packages, setup

setup(
    name="sphere_nerf_mod",
    version="0.0.1",
    description="Nerf with coordinates located on the spheres",
    author="ktylus, waczyjoan",
    packages=find_packages(),
)
